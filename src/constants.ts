export const damageRangeMax = 255;
export const damageRangeMin = 217;
export const defaultIv = 15; // Assume max possible.
export const defaultEv = 1; // Assume no effort value.
export const defaultLevel = 5;
