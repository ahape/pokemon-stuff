export const damageRangeMax = 255;
export const damageRangeMin = 217;
export const maxDv = 15;
export const minDv = 0;
export const defaultDv = 8;
// Assume no effort value, since that's incredibly hard to quantify.
// But we do know that opponent Pokemon have none.
export const defaultEv = 0;
export const defaultLevel = 5;
